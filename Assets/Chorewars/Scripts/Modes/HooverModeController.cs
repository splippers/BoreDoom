namespace Chorewars.Modes
{
    public class HooverModeController : BaseCoverageModeController
    {
        // Configure this in the Unity Inspector (mode id, export prefix, chore asset, etc.)
    }
}
